title: terraform-push-state
author: drone-plugins
tags:
  - infrastructure
  - harness
  - terraform
logo: terraform.svg
repo: https://github.com/drone-plugins/drone-terraform
image: https://hub.docker.com/r/plugins/drone-terraform
license: None
readme: https://github.com/drone-plugins/drone-terraform
description: >-
  This terraform plugin will push the raw state from terraform.tfstate to the
  IAC Server
example: |
  version: 1
  name: default
  steps:
    - name: Init
      type: plugins/harness-terraform

properties:
  operation:
    type: string
    defaultValue: 'push-state'
    description: Command that indicates what action the plugin will perform
    secret: false
    required: true
